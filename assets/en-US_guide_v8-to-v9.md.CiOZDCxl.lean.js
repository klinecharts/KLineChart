import{a as o,c as a,a2 as d,o as t}from"./chunks/framework.D0LcRxHx.js";const u=JSON.parse('{"title":"🛠️ From V8 to V9","description":"","frontmatter":{},"headers":[],"relativePath":"en-US/guide/v8-to-v9.md","filePath":"en-US/guide/v8-to-v9.md","lastUpdated":1709219794000}'),c={name:"en-US/guide/v8-to-v9.md"};function i(n,e,l,r,s,h){return t(),a("div",null,e[0]||(e[0]=[d('<h1 id="🛠️-from-v8-to-v9" tabindex="-1">🛠️ From V8 to V9 <a class="header-anchor" href="#🛠️-from-v8-to-v9" aria-label="Permalink to &quot;🛠️ From V8 to V9&quot;">​</a></h1><p>This document will help you upgrade from kinecharts 8.x to klinecharts 9.x. If you are 7.x or older, please refer to the previous upgrade document to upgrade to 8.x.</p><h2 id="import-adjustment" tabindex="-1">Import adjustment <a class="header-anchor" href="#import-adjustment" aria-label="Permalink to &quot;Import adjustment&quot;">​</a></h2><p><code>klinecharts/index.blank&#39;</code> and <code>klinecharts/index.simple</code> are no longer distinguished. Please use <code>import {...} from klinecharts</code> uniformly.</p><h2 id="design-adjustment" tabindex="-1">Design adjustment <a class="header-anchor" href="#design-adjustment" aria-label="Permalink to &quot;Design adjustment&quot;">​</a></h2><p><code>shape</code>, <code>annotation</code>, <code>tag</code> are merged into <code>overlay</code>. Please refer to <a href="./overlay">overlay</a> for details.</p><h2 id="style-configuration-adjustment" tabindex="-1">Style configuration adjustment <a class="header-anchor" href="#style-configuration-adjustment" aria-label="Permalink to &quot;Style configuration adjustment&quot;">​</a></h2><ul><li>The style options of all lines are changed to <code>dashed</code> and <code>dashValue</code> to <code>dashedValue</code>.</li><li><code>candle.tooltip.labels</code> and <code>candle.tooltip.values</code> are merged into <code>candle.tooltip.custom</code>.</li><li><code>xAxis.height</code> is changed to <code>xAxis.size</code>, <code>xAxis.tickeText.paddingTop</code> is changed to <code>xAxis.tickeText.marinStart</code>, and <code>xAxis.tickeText.paddingBottom</code> is changed to <code>xAxis.tickeText.marginEnd</code>.</li><li><code>yAxis.height</code> is changed to <code>yAxis.size</code>, <code>yAxis.tickeText.paddingTop</code> is changed to <code>yAxis.tickeText.marinStart</code>, and <code>yAxis.tickeText.paddingBottom</code> is changed to <code>yAxis.tickeText.marginEnd</code>.</li><li><code>technicalIndicator.bar</code> is changed to <code>indicator.bars</code>, <code>technicalIndicator.line</code> is changed to <code>indicator.lines</code>，<code>technicalIndicator.circle</code> is changed to <code>indicator.circles</code>.</li><li>Delete <code>shape</code>, <code>annotation</code>, <code>tag</code>, please use <code>overlay</code> instead.</li></ul><h2 id="api-adjustment" tabindex="-1">API adjustment <a class="header-anchor" href="#api-adjustment" aria-label="Permalink to &quot;API adjustment&quot;">​</a></h2><h3 id="chart-api" tabindex="-1">Chart API <a class="header-anchor" href="#chart-api" aria-label="Permalink to &quot;Chart API&quot;">​</a></h3><ul><li><code>extension.addTechnicalIndicatorTemplate(template)</code> is changed to <code>registerIndicator(template)</code>.</li><li>Delete <code>extension.addShapeTemplate(template)</code>, please use <code>registerOverlay(template)</code> instead.</li></ul><h3 id="instance-api" tabindex="-1">Instance API <a class="header-anchor" href="#instance-api" aria-label="Permalink to &quot;Instance API&quot;">​</a></h3><ul><li><code>getDom({paneId, position})</code> is changed to <code>getDom(paneId, position)</code>, and the parameter <code>position</code> option is changed to <code>root</code>, <code>main</code> and <code>yAxis</code>.</li><li><code>getWidth()</code> and <code>getHeight()</code> are combined into a method &#39;<code>getSize(paneId, position)</code>.</li><li><code>setStyleOptions(styles)</code> is changed to <code>setStyles(styles)</code>.</li><li><code>getStyleOptions()</code> is changed to <code>getStyles()</code>.</li><li><code>setOffsetRightSpace(space)</code> is changed to <code>setOffsetRightDistance(distance)</code>.</li><li><code>createTechnicalIndicator(value, isStack, paneOptions)</code> is changed to <code>createIndicator(value, isStack, paneOptions)</code>.</li><li><code>overrideTechnicalIndicator(override, paneId)</code> is changed to <code>overrideIndicator(override, paneId)</code>.</li><li><code>getTechnicalIndicatorByPaneId(paneId, name)</code> is changed to <code>getIndicatorByPaneId(paneId, name)</code>.</li><li><code>removeTechnicalIndicator(paneId, name)</code> is changed to <code>removeIndicator(paneId, name)</code>.</li><li><code>subscribeAction(type, callback)</code> and <code>unsubscribeAction(type, callback)</code>. The parameter <code>type</code> option is changed to <code> onZoom</code>, <code>onScroll</code>, <code>onCrosshairChange</code>, <code>onVisibleRangeChange</code> and <code>onPaneDrag</code>.</li><li><code>convertToPixel(value, finder)</code> and <code>convertFromPixel(coordinate, finder)</code>, the parameter <code>finder.absoluteYAxis</code> is changed to <code>finder.absolute</code>.</li><li>To delete <code>createShape(value, paneId)</code>, <code>createAnnotation(annotation, paneId)</code>, <code>createTag(tag, paneId)</code>, please use <code>createOverlay(value, paneId)</code> instead.</li><li>To delete <code>removeShape(id)</code>, <code>removeAnnotation(paneId, points)</code>, <code>removeTag(paneId, tagId)</code>, please use <code>removeOverlay(id)</code> instead.</li><li>To delete <code>setShapeOptions(options)</code>, use <code>overrideOverlay(override)</code> instead.</li><li>To delete <code>createHtml(html, paneId)</code>, <code>removeHtml(paneId, htmlId)</code>, please obtain the corresponding dom through <code>getDom(paneId, position)</code>.</li><li>To delete <code>getTechnicalIndicatorByPaneId(paneId, name)</code>.</li></ul><h3 id="shape-help-api" tabindex="-1">Shape help API <a class="header-anchor" href="#shape-help-api" aria-label="Permalink to &quot;Shape help API&quot;">​</a></h3><ul><li>All APIs are migrated to &#39;klinecharts.utils&#39;.</li></ul><h2 id="customized-technical-indicator-adjustment" tabindex="-1">Customized technical indicator adjustment <a class="header-anchor" href="#customized-technical-indicator-adjustment" aria-label="Permalink to &quot;Customized technical indicator adjustment&quot;">​</a></h2><ul><li>Attribute <code>plots</code> is changed to <code>figures</code>, sub item methods <code>color</code>, <code>isStroke</code>, <code>isDashed</code> are merged into <code>styles</code>.</li><li>Change the method &#39;<code>regeneratePlots(params)</code> to <code>regenerateFigures(params)</code>.</li><li>The method <code>calcTechnicalIndicator(kLineDataList, options)</code> is changed to <code>calc(kLineDataList, indicator)</code>.</li><li>The method <code>createTooltipDataSource({ dataSource, viewport, crosshair, technicalIndicator, xAxis, yAxis, defaultStyles })</code> is changed to <code>createTooltipDataSource({ kLineDataList, indicator, visibleRange, bounding, crosshair, defaultStyles, xAxis, yAxis })</code>. The return value is determined by <code>[{ title: &#39;xxx&#39;, value: &#39;xxx&#39;, color: &#39;xxx&#39; } ]</code> Change to <code>{ name: &#39;xxx&#39;, calcParamsText: &#39;xxx&#39;, values: [{ title: &#39;xxx&#39;, value: &#39;xxx&#39;, color: &#39;xxx&#39; }, ...] }</code>.</li><li>The method <code>render({ctx, dataSource, viewport, styles, xAxis, yAxis})</code> is changed to <code>draw({ ctx, kLineDataList, indicator, visibleRange, bounding, barSpace, defaultStyles, xAxis, yAxis })</code>.</li><li>Delete the attribute <code>shouldCheckParamCount</code>.</li></ul>',17)]))}const m=o(c,[["render",i]]);export{u as __pageData,m as default};
